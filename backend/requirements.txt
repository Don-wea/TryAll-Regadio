# Dependencias básicas para el proyecto Django

Django>=5.0.6

# Herramientas útiles para desarrollo (opcional, puedes quitarlas si no quieres)
django-environ>=0.11.2    # para manejar variables de entorno desde .env
djangorestframework>=3.15.1  # si planean usar API REST
python-decouple>=3.8      # alternativa para gestión de variables de entorno
Pillow>=10.3.0            # para trabajar con imágenes
